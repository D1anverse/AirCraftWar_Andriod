<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAirCraftWar">

        <!-- 启动 Activity：MenuActivity -->
        <activity
            android:name=".application.MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 游戏界面 Activity -->
        <activity
            android:name=".application.GameActivity"
            android:exported="false" />

        <!-- 排行榜 Activity -->
        <activity
            android:name=".application.ScoreActivity"
            android:exported="false" />

        <activity android:name=".application.HeroSelectActivity"
            android:exported="false"/>

    </application>

</manifest>